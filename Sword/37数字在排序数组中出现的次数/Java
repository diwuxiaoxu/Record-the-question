题目描述
统计一个数字在排序数组中出现的次数。

解析：

  直接顺序搜索
  
public class Solution {
    public int GetNumberOfK(int [] array , int k) {
        int res = 0;
       for(int n : array){
           if(n == k) res++;
       }
        return res;
    }
}
